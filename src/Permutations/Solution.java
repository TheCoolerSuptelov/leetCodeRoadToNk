package Permutations;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class Solution {

   /* public static void main(String[] args) {
        var arrTest = new int[]{1,2,3};
        var xx = permute(arrTest);
        for (int i = 0; i < xx.size(); i++) {
           xx.get(i).stream().map(e->e.toString()).collect(Collectors.joining(","));
            System.out.println("###############");
        }
    }
    public static List<List<Integer>> permute(int[] nums) {
        var rootArray = new ArrayList<ArrayList<Integer>>();
        var newArr = new ArrayList<Integer>();
        for (int i = 0; i < nums.length; i++) {

            for (int j = i+1; j > nums.length; j++) {

            }
            rootArray.add(newArr);
            newArr.clear();
        }
    }*/
}
